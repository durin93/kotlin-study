fun main(args: Array<String>) {
    var a1 = 100

    if(a1 > 50){
        println("a1은 50보다 큽니다.")
    }

    //if else는 같다 자바랑

    //when 은 자바의 switch case와 비슷
    //수식이나 값의 결과에 따라 수행될 부분을 결정, 자바와 다르게 정수 뿐만 아니라
    //다양한 타입의 값을 비교 할 수 있다.
    //when(변수 or 수식){
    // 값1 - > 코드
    // 값2  -> 코드




}